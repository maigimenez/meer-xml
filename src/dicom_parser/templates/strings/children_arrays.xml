
    <!-- Children Arrays -->
    {%- for node in nodes %}
     <string-array name="list_{{ node['parent_code'] }}">
        {%- for child in node['children'] %}
        <item>{{ child }}</item>
        {%- endfor %}
    </string-array>
    {% endfor %}
