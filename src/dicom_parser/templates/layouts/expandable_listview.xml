
                        <!-- Children list -->
                        <ExpandableListView
                            android:id="@+id/list_{{ concept_value }}"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_centerVertical="true"
                            android:groupIndicator="@null">
                        </ExpandableListView>